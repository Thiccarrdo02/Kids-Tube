# Backend URL for the deployed admin panel. Used at build time.
# Override locally before building, or set BACKEND_URL in the CI env.
# Trailing slash is fine; the app normalizes it.
BACKEND_URL=https://kids-tube-lyart.vercel.app
